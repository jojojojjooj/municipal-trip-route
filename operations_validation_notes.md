# 통합 현장 운영 고도화 검증 기록

검증일: 2026-08-22

| 구분 | 검증 내용 | 결과 |
| --- | --- | --- |
| 데스크톱 운영 현황 | 계획 준비도 아래에 진행률, 다음 방문지, 이슈 수, 체크리스트가 표시됨 | 통과 |
| 목적지 실행 제어 | 각 목적지에 예정·진행·완료·이슈 상태, 예상 도착 시각 및 이슈 기록 입력 영역이 표시됨 | 통과 |
| 출력 제어 | 경로 요약에 PDF·인쇄·공유·CSV 제어가 표시됨 | 통과 |
| PDF 보고서 | 화면의 보고서 미리보기에서 실행 진행률·체크리스트·목적지별 예상 도착 시각과 상태가 반영됨 | 통과 |
| 모바일 390px | 운영 대시보드와 모바일 출력 작업 바가 단일 열 흐름으로 표시됨 | 통과 |

실행 상태 API, 체크리스트 API, 일정 예측, CSV 생성은 자동 회귀 테스트로 검증한다. 저장된 계획에서의 실제 API 갱신·복제·CSV 다운로드 흐름은 다음 검증 단계에서 추가 확인한다.

브라우저 자동화에서 상태 버튼 클릭 직후 같은 실행 틱의 DOM 텍스트는 React 렌더 갱신 전 값으로 남을 수 있음을 확인했다. 이 동작은 상태 집계·API 위임 단위 테스트로 검증하며, 다음 화면 갱신 확인에서 상태 전환 결과를 다시 관찰한다.

추가 화면 갱신 확인에서 첫 목적지를 `완료`로 처리하자 전체 진행률이 0%에서 50%로, 다음 방문지가 첫 번째에서 두 번째 목적지로 바뀌었고 완료 시각이 표시됐다. 화면 내 PDF 보고서 미리보기에도 완료 상태·완료 시각·50% 진행률이 함께 갱신되는 것을 확인했다.

`출발 전 확인` 체크리스트를 전환한 뒤 완료 수가 0/3에서 1/3으로 바뀌고 완료 스타일이 적용되는 것을 확인했다.

CSV 제어는 목적지 운영 목록을 Blob 파일로 생성하고 성공 안내를 표시했다. 브라우저 자동화에서는 다운로드 목록 반영을 즉시 관찰하지 못해 Blob URL 해제를 1초 뒤로 지연했으며, 보완 후 버튼 실행과 `목적지 운영 목록을 CSV로 내보냈습니다.` 성공 안내를 확인했다.

저장 계획 복제를 직접 검증하기 위해 실제 계정 플래너에서 `통합운영 복제 검증` 계획의 기본 정보를 입력했다. 다음 단계에서 두 목적지를 추가하고 원본 저장·복제·정리까지 수행한다.

실제 계정 화면에서 주소 검색 입력 후 자동 저장 상태는 브라우저·계정 보관됨으로 전환됐다. 주소 검색 결과는 다음 상호작용에서 입력값과 요청 상태를 확인한 뒤 목적지 추가를 이어간다.

브라우저 자동 입력이 검색 필드의 React 상태에 즉시 반영되지 않아 네이티브 입력 이벤트로 값 반영을 재확인했다. 입력값은 반영됐지만 이 테스트 세션에서는 검색 결과 버튼이 나타나지 않아, 복제 검증은 데이터 API 대신 기존 동작 범위를 자동 테스트로 보강하거나 별도 저장 계획을 이용해 이어서 확인한다.

검색 결과가 렌더링된 뒤 `서울특별시청`을 선택해 실제 계획에 첫 목적지를 추가했다. 준비도는 2/3, 운영 대시보드의 다음 방문지는 서울특별시청, 방문 목적지 수는 1곳으로 즉시 갱신됐다.

두 번째 주소 `서울역`의 검색 결과가 정상적으로 표시됐으며, 첫 번째 결과를 선택해 두 목적지 계획을 완성할 준비가 됐다.

`서울역`까지 추가한 두 목적지 계획을 실제 저장했고, 저장 성공 안내와 Archive 카드의 `불러오기·복제·삭제` 제어가 표시되는 것을 확인했다.

Archive의 `복제` 제어를 실행한 뒤 500ms 후에도 제목과 목적지 수가 원본 상태로 남아 있어, 복제 핸들러의 데이터 조회·오류 피드백을 추가 진단한다.

복제 버튼 이벤트를 직접 실행하고 1.2초를 대기한 결과, 제목이 `통합운영 복제 검증 사본`으로 바뀌었고 목적지 2곳이 유지되며 `저장 계획을 복제했습니다. 새 계획으로 수정 후 저장하세요.` 안내가 표시됐다. Archive 카드 수는 1건으로 유지되어 복제는 새 미저장 계획을 만드는 의도대로 동작했다.

복제된 계획에서 CSV를 실행한 뒤 브라우저 다운로드 목록에 `통합운영-복제-검증-사본-2026-08-22-목적지.csv` 파일이 새 항목으로 생성됐다. 기존 파일의 파일명 정제와 실제 다운로드를 함께 확인했다.

실제 플래너를 다시 열어 복제된 임시 초안을 복원했고, 제목·두 목적지·예상 도착 시각이 보존된 상태에서 390px 직접 조작 검증을 준비했다.

실제 390px 조건에서 모바일 작업 바가 표시됨을 확인한 뒤, 첫 목적지를 완료 처리하고 출발 전 체크리스트를 전환했으며 CSV 제어를 실행했다. 진행률은 50%, 다음 방문지는 서울역, 체크리스트는 1/3 완료로 갱신됐고 CSV 성공 안내도 표시됐다.

같은 390px 조건에서 Archive의 복제 제어를 직접 실행해 `통합운영 복제 검증 사본` 제목, 두 목적지 유지, 새 미저장 계획 안내를 확인했다.

검증을 위해 저장한 원본 계획은 삭제 성공 안내와 함께 Archive에서 0건으로 정리했고, 복제된 임시 초안도 브라우저·계정 저장소에서 비웠다.

협업·운영 보드 고도화 후 데스크톱 전체 화면에서 `운영 보드` 탭, 최근 6개월 완료·이슈 막대 영역, 통합 검색, 이슈 빈 상태가 기존 Field Dossier 색상·정보 위계를 유지하며 표시되는 것을 확인했다. 저장된 운영 데이터가 없는 계정에서도 지표는 0건으로 실제 값만 표시하며, 임의 데이터를 추가하지 않는다.

390px 모바일 전체 화면에서 운영 지표 4개, 월별 분석, 통합 검색, 현장 이슈 보드가 단일 열 흐름으로 겹침 없이 표시되는 것을 확인했다. `pnpm check`와 17개 테스트 파일·47개 테스트를 통과했으며, 권한 API는 소유자·편집자·열람자 검증 경로를 포함한 라우터 테스트로 회귀 검증했다.

결과 보고서 초안 화면은 데스크톱에서 출장 개요·수행 결과·이슈 및 조치·후속 계획의 4개 편집 영역과 자동 생성·저장·PDF 출력 제어가 한 화면에 표시됨을 확인했다. 390px 모바일에서도 지표·제어·4개 편집 영역이 단일 열로 재배치되어 텍스트 입력과 출력 제어가 겹치지 않았다.

사진이 있는 개발 미리보기에서 목적지별 설명·촬영일·실행 상태를 기준으로 총 3장의 현장 증빙 사진이 결과 보고서 본문 미리보기와 PDF 문서에 자동 선정되는 구조를 확인했다. 데스크톱에서는 3열 증빙 카드, 390px 모바일에서는 단일 열 카드로 재배치되어 촬영일·설명·목적지 정보가 잘리지 않고 표시됐다.

사진 자동 배치 카드는 데스크톱에서 `DRAG TO REORDER` 안내와 함께 3열 드래그 대상 카드로 표시되며, 순서 변경 뒤 초안 저장 시 PDF에도 같은 순서가 반영되도록 연결했다. 390px 모바일에서는 카드가 단일 열로 재배치되고 안내·사진·메타데이터가 겹치지 않음을 확인했다.

모바일 사진 카드에는 현재 순서와 위·아래 이동 버튼을 추가했다. 첫 사진의 위 이동과 마지막 사진의 아래 이동은 비활성으로 표시되고, 다른 버튼은 40px 이상의 터치 영역으로 표시됨을 390px 화면에서 확인했다. 데스크톱에서도 드래그 안내와 이동 버튼이 함께 노출되어 두 방식으로 순서를 조정할 수 있다.

사진별 보고서 포함 토글은 데스크톱 3열 카드와 390px 단일 열 카드에서 모두 표시됐다. 토글은 현재 포함 수를 상단에 반영하고, 비포함 사진은 흐린 점선 카드로 구분되도록 구성했으며, 제외 키는 초안 저장 후 PDF 본문 필터에 적용되도록 테스트했다.

결과 보고서 작업 화면에 `결과 HWPX` 다운로드 제어를 추가했다. 데스크톱과 390px 화면에서 PDF 제어와 나란히 표시되고, 작은 화면에서도 버튼과 편집 영역이 겹치지 않았다. 자동 테스트는 HWPX ZIP 내부의 `mimetype`, 컨테이너, manifest, header, section XML을 확인하고, 보고서 4개 문안과 현재 포함된 사진의 방문 순서·목적지·촬영일·설명이 본문 XML에 반영되는 것을 검증했다.

HWPX 문서 미리보기는 결과 보고서 편집 영역 아래에서 접어 두었다가 필요할 때 열 수 있게 구성했다. 펼친 상태의 데스크톱 화면에서는 제목·기본 정보·4개 문안·현장 증빙 목록이 한글 문서 본문 순서로 A4형 표면에 표시됐고, 390px 화면에서는 기본 정보와 증빙 목록이 한 열로 재배치되어 잘림 없이 확인됐다. 현재 문안, 포함 토글 및 사진 순서를 바꾸면 같은 데이터가 미리보기와 HWPX 생성기에 함께 전달된다.
