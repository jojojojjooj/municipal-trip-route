# 디자인·UI 고도화 진단

검토일: 2026-08-22

| 영역 | 현재 강점 | 개선 우선순위 |
| --- | --- | --- |
| 출장 설계 | 크림·다크그린·테라코타의 공공 현장 대시보드 정체성이 분명하며, 준비도·출발지·경로 요약 정보가 존재함 | 지도 영역을 실제 업무 표면처럼 강화하고, 입력·목적지·요약의 단계적 흐름과 주요 행동을 더 선명하게 구분 |
| 현장 기록 | 카드형 기록과 검색·필터·선택 PDF 기능이 안정적으로 연결됨 | 필터·선택·출력 제어를 하나의 증거 보관 도구 모음으로 정리하고, 결과 수·선택 수를 빠르게 판독할 수 있게 강화 |
| 데스크톱 | 좌측 입력–중앙 지도–우측 요약의 업무 구조가 명확함 | 화면 중앙의 지도 밀도가 상대적으로 낮으므로 좌표·경로·상태 정보를 보강하고, 섹션별 표면 대비를 확대 |
| 390px 모바일 | 한 열 흐름과 현장 기록 가로 카드가 작동함 | 긴 설계 폼의 스캔 부담을 줄이기 위해 구역 헤더·주요 행동의 고정성·카드 그룹화를 높이고, 필터·PDF 제어의 터치 영역을 강화 |

개선 방향은 **Municipal Field Dossier**로 유지한다. 따뜻한 종이 질감, 얇은 규칙선, 다크그린 잉크, 테라코타 신호색, 수평형 한글 타이포그래피를 보존하면서 지도와 기록을 현장 운영의 판단·증빙 표면으로 발전시킨다.

## 구현 기준

1. 출장 설계는 `기본 정보 → 출발 기준 → 목적지 → 최적화/저장` 순서가 한눈에 이어지도록 구역 표면과 행동 우선순위를 정돈한다.
2. 지도에는 좌표 프레임, 운영 상태, 활성 경로의 시각적 단서를 추가해 입력 폼의 여백과 균형을 맞춘다.
3. 현장 기록은 `검색 조건 → 선택 현황 → PDF 출력`을 하나의 보관 작업대처럼 묶고, 카드의 메타데이터 판독성을 강화한다.
4. 모바일에서는 필터·주요 저장 및 출력 제어를 넓은 터치 영역과 명확한 구분선으로 제공하며, 긴 폼에서도 다음 행동이 예측 가능해야 한다.

## 반영 후 확인

| 화면 | 확인 결과 |
| --- | --- |
| 데스크톱 설계 | 준비도 패널·입력 도시에·지도·경로 요약이 표면 대비와 상단 신호선으로 분리되며, 경로 요약은 화면 스크롤 중에도 참조 가능한 고정 패널로 정돈됨 |
| 390px 설계 | 지도 연결 오류 상태가 격자형 복구 프로토콜 패널로 구분되고, 설계 폼·경로 요약의 순서와 주요 저장 제어가 한 열 흐름에서 유지됨 |
| 390px 현장 기록 | 검색 조건, 선택 현황·PDF 출력, 가로형 증거 카드가 각각 별도 표면으로 구분되어 스캔과 터치 조작의 우선순위가 개선됨 |
| 390px 고정 작업 바 | 최초 화면의 하단에 저장·PDF·인쇄·공유 제어가 고정 표면으로 표시되며, 준비도·지도 오류·기본 정보의 스크롤 흐름을 가리지 않음 |

고정 작업 바의 저장·PDF·인쇄·공유 버튼은 기존에 브라우저 검증된 동일 핸들러를 재사용한다. 별도 자동화 브라우저는 세션 쿠키 없이 로그인 화면으로 전환되어 상태 변경 클릭은 수행하지 못했으나, 로그인된 검증 화면에서 고정 작업 바의 표시와 터치 영역을 390px 스크린샷으로 확인했다.

추가 검증에서 로그인된 미리보기의 촬영일 필터를 `2026-08-21`로 변경해 3건 중 2건으로 결과가 즉시 갱신되는 것을 확인한 뒤 초기화했다. 또한 모바일 작업 바의 `PDF`와 `공유` 제어를 직접 실행해 기존 출력 핸들러와 저장 전 공유 안내 핸들러에 연결됐음을 확인했다. 검증 중에는 데스크톱 폭에서 작업 바를 일시 노출했으며, 실제 390px 표시는 별도 스크린샷으로 확인했다.

모바일 고정 작업 바는 `saveTrip`, `downloadPdf`, 인쇄, `shareTrip`의 기존 처리기를 그대로 사용하며, 640px 이하에서만 고정 표시되도록 구성했다. 이에 대한 연결 규칙과 하단 안전 영역·스크롤 여백은 `server/mobileActionDock.test.ts`로 회귀 검증한다.

실제 로그인된 Chromium을 `390×844` 기기 메트릭으로 전환한 뒤 `window.matchMedia('(max-width: 640px)')`가 `true`이고 고정 작업 바가 `display: block`임을 확인했다. 이 상태에서 고정 작업 바의 PDF 실행은 **“출장 경로 요약 PDF를 다운로드했습니다.”** 성공 메시지를 반환했고, 저장 실행은 **“출장 계획을 저장했습니다.”** 메시지와 저장 목록 항목을 반환했다. 현장 기록의 날짜 필터도 3건에서 2건으로 즉시 갱신됐으며, 공유 제어는 저장된 계획에서 실행해 링크 복사 처리기에 연결됨을 확인했다.
