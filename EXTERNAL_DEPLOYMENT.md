# 외부 도메인 배포 안내

이 패키지는 React·Vite 프런트엔드와 Express·tRPC 서버, MySQL/TiDB 데이터베이스를 함께 사용하는 애플리케이션입니다. `node_modules`, 빌드 결과물, 실행 로그, 로컬 환경파일 및 실제 데이터는 소스 ZIP에 포함하지 않습니다.

> **중요:** 이 문서는 소스 이전을 위한 안내입니다. 기존 서비스의 사용자·출장 계획·사진·데이터베이스 데이터와 보안 환경변수는 별도로 이전해야 하며, ZIP만으로 복제되지 않습니다.

## 1. 준비 사항

| 항목 | 권장 구성 | 용도 |
|---|---|---|
| 런타임 | Node.js 22 이상, pnpm 10 | 빌드 및 서버 실행 |
| 데이터베이스 | MySQL 또는 TiDB | 계획·목적지·현장 기록·권한 데이터 저장 |
| HTTPS 도메인 | `https://your-domain.example` | 지도 SDK·로그인·보안 쿠키 운영 |
| 카카오 개발자 앱 | JavaScript 키 및 REST API 키 | 지도 렌더링과 주소 검색 |
| OAuth 제공자 | 최종 도메인 콜백 등록 | 로그인 유지 |
| 파일 저장소 | S3 호환 저장소 또는 기존 저장소 연동 | 현장 사진 보관 |

## 2. 소스 설치 및 실행

압축을 해제한 프로젝트 폴더에서 아래 순서로 설치합니다. 로컬에서는 아래 환경변수 표를 기준으로 별도 `.env` 파일을 직접 만들고, 운영 서버에서는 호스팅 서비스의 비밀 환경변수 설정 화면에 동일한 값을 등록합니다. 실제 `.env` 파일은 보안상 소스 ZIP에 포함하지 않습니다.

```bash
pnpm install --frozen-lockfile
cp .env.example .env
pnpm check
pnpm test
pnpm build
pnpm start
```

호스팅 서비스가 `PORT` 값을 전달하는 경우 해당 값을 그대로 사용합니다. 서버는 전달된 포트를 우선 사용하며, 운영 모드에서는 빌드된 정적 프런트엔드와 API를 같은 서버에서 제공합니다.

## 3. 환경변수 설정

실제 비밀값은 저장소에 커밋하거나 소스 ZIP에 넣지 마십시오. 아래 표의 변수 이름만 각 배포 환경의 비밀 설정 화면에 등록합니다.

| 구분 | 환경변수 | 필수 여부 | 설명 |
|---|---|---:|---|
| 데이터 | `DATABASE_URL` | 필수 | 운영 MySQL/TiDB 연결 문자열 |
| 보안 | `JWT_SECRET` | 필수 | 충분히 긴 무작위 세션 서명 비밀값 |
| 지도 | `VITE_KAKAO_MAP_APP_KEY` | 필수 | 브라우저에서 사용하는 카카오 JavaScript 키 |
| 지도 | `KAKAO_REST_API_KEY` | 필수 | 서버에서만 사용하는 카카오 로컬 REST 키 |
| 로그인 | `VITE_APP_ID`, `OAUTH_SERVER_URL`, `VITE_OAUTH_PORTAL_URL` | 필수 | 현재 로그인 연동에 필요한 OAuth 설정 |
| 로그인 | `OWNER_OPEN_ID`, `OWNER_NAME` | 권장 | 초기 소유자 식별과 운영 표시 |
| 사진 저장 | `BUILT_IN_FORGE_API_URL`, `BUILT_IN_FORGE_API_KEY` | 현 기능 유지 시 필수 | 현재 사진 업로드 프록시의 서버 측 저장소 연동 |
| 사진 저장 | `VITE_FRONTEND_FORGE_API_URL`, `VITE_FRONTEND_FORGE_API_KEY` | 현 기능 유지 시 필수 | 프런트엔드 저장소 연동 설정 |
| 분석 | `VITE_ANALYTICS_ENDPOINT`, `VITE_ANALYTICS_WEBSITE_ID` | 선택 | 분석 스크립트 사용 시 설정 |

`VITE_` 접두사가 있는 값은 프런트엔드 번들에 노출될 수 있으므로, REST 키·데이터베이스 비밀번호·세션 비밀값에는 절대 사용하지 않습니다.

## 4. 데이터베이스 이전

새 데이터베이스를 준비한 뒤 `DATABASE_URL`을 설정하고, 포함된 `drizzle/` 스키마와 마이그레이션을 적용합니다. 기존 운영 데이터를 계속 사용할 경우에는 현재 데이터베이스의 백업·복원 절차를 별도로 수행해야 합니다. 데이터 이전 전에는 목적지, 현장 사진 메타데이터, 협업 권한 및 임시 초안 테이블을 포함하는지 확인하십시오.

```bash
pnpm db:push
```

운영 데이터베이스에 적용하기 전에는 반드시 백업본 또는 별도 검증 데이터베이스에서 마이그레이션을 점검하십시오.

## 5. 사용자 도메인 연결 순서

1. 선택한 호스팅 서비스에 소스를 배포하고 빌드 명령을 `pnpm build`, 시작 명령을 `pnpm start`로 설정합니다.
2. 호스팅 서비스가 안내하는 DNS 레코드를 도메인 등록기관에 추가하고 HTTPS 인증서 발급을 완료합니다.
3. 최종 HTTPS 도메인을 카카오 개발자 콘솔의 **웹 플랫폼 사이트 도메인**에 등록합니다. `www` 도메인을 사용할 경우에도 별도로 등록합니다.
4. OAuth 제공자에 `https://최종도메인/api/oauth/callback` 콜백 주소를 등록하고, 앱의 OAuth 환경변수를 새 값으로 갱신합니다.
5. 최종 도메인에서 로그인, 주소 검색, 지도 렌더링, 사진 업로드, 저장 계획 조회를 순서대로 점검합니다.

## 6. 외부 운영 시 확인할 항목

현재 소스는 관리형 OAuth 및 사진 저장소 연동을 사용합니다. 외부 호스팅에서 같은 서비스를 계속 사용하려면 해당 제공자의 외부 도메인·콜백·API 접근 권한을 먼저 확인해야 합니다. 사용할 수 없는 경우에는 OAuth를 외부 인증 제공자로 교체하고, `server/storage.ts`의 사진 저장소 연동을 독립 S3 호환 저장소로 바꿔야 합니다.

카카오 REST API 키는 서버 환경변수에만 넣고, JavaScript 키는 등록한 HTTPS 도메인에서만 사용하십시오. 배포 직후에는 브라우저 개발자 도구와 서버 로그에 키·토큰·연결 문자열이 출력되지 않는지도 확인해야 합니다.
